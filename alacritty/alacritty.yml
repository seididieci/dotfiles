shell:
  program: /bin/zsh
  args:
    - -l
    - -c
    - "tmux attach || tmux"

window:
  # Seting some terminal opacity
  opacity: .8

font:
  size: 10.5

colors:
  primary:
    background: '#000000'

key_bindings:
 - { key: Return, mods: Control|Shift, action: SpawnNewInstance }

